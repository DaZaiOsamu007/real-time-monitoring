services:
  - type: web
    name: real-time-monitoring
    env: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 8050
